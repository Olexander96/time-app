# образ, на основі якого створюється наш образ (цей образ дозволяє нам виконати npm команди!)
FROM node:alpine

WORKDIR /app
# публікуємо порт для доступу зовні
EXPOSE 3000
# копіюємо package.json, package-lock.json в корінь образа
COPY package*.json ./
# коканда яку потрібно запістити перед запуском в контейнері (буде дивитись на package.json), щоб потім працював додаток
RUN npm install
# копіюємо всі файли поточної папки в образ в папку ./app
COPY . ./
# команда яка виконається при створенні контейнера з образа (команда run dev прописана в package.json)
CMD [ "npm", "run", "dev" ]